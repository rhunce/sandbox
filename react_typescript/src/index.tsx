import React from 'react';
import ReactDOM from 'react-dom';
import App from './useReducer/components/App';
// Modify above line to show App from different directories

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);